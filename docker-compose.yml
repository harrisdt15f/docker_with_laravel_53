bocat2_0:
  build: ./www
  container_name: bocat2_0
  restart: always
  ports:
    - "80:80" # web
  links:
#    - database:database
    - redis:redis
    - php56:php56
#    - memcached:memcached
  volumes:
    - /opt/lampp/htdocs:/var/www/html
    # - ./nginx/vhost.conf:/etc/nginx/conf.d/vhost.conf

#database:
#  build: ./database
#  container_name: database
#  restart: always
#  ports:
#    - "3306:3306"
#  volumes:
#    - ./store/database:/var/lib/mysql
#  env_file:
#    - .env

redis:
  build: ./redis
  container_name: redis
  restart: always
  ports:
    - "6379:6379"

php56:
  build: ./php56fpm
  container_name: php56fpm
  restart: always
  ports:
    - "9000:9000"

#memcached:
#  build: ./memcached
#  container_name: memcached
#  restart: always
#  ports:
#  - "11211:11211"
